<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
</a>
 
<!-- Be sure to leave the brand out there if you want it shown -->
<div class="brand"> <%= link_to "After-hours-cliq", root_path %></div>
 
<!-- Everything you want hidden at 940px or less, place within here -->
<div class="nav-collapse collapse">
<!-- .nav, .navbar-search, .navbar-form, etc -->
    <ul class="nav">
        <li <%= active(root_path) %> ><%= link_to "Home", root_path %></li>
        <% if current_user %>
            <li><%= link_to("Sign out", destroy_user_session_path, :method=>'delete') %></li>
            <% if current_user.userdetail.nil? %>
                <li <%= active(new_userdetail_path) %> ><%= link_to('Profile', new_userdetail_path) %></li>
            <% else %>
                <li <%= active(edit_userdetail_path(current_user)) %> ><%= link_to('Profile', edit_userdetail_path(current_user)) %> </li>
            <% end %>

        <% else %>    
            <li <%= active(new_user_session_path) %> ><%= link_to "Sign in", new_user_session_path %></li>
            <li <%= active(new_user_registration_path) %> ><%= link_to "Sign up", new_user_registration_path %></li>
        <% end %>    
        
     </ul>   
</div>